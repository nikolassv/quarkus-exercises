package com.example.config;

import io.smallrye.config.ConfigMapping;

/**
 * TODO Task 6: Implement this as a ConfigMapping that covers all {@code engine.*}
 * properties and inject it into {@link com.example.engine.AetherEngineService}
 * in place of the individual {@code @ConfigProperty} fields.
 */
public interface EngineConfig {
}
