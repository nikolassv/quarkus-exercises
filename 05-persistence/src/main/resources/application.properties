quarkus.application.name=grimoire-library

# Database schema: recreate on every startup (fine for dev/test — never for prod)
quarkus.hibernate-orm.database.generation=drop-and-create

# Render dates as ISO strings instead of numeric arrays
quarkus.jackson.write-dates-as-timestamps=false
